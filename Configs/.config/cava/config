## Файл конфигурации для CAVA.
# Удалить ;  изменить параметры.

[general]

# Режим сглаживания.  Может быть «нормальным», «научным» или «волновым».  УСТАРЕЛО с версии 0.6.0
; mode = normal

# Принимает только неотрицательные значения.
; framerate = 60

# «autosens» попытается снизить чувствительность, если столбики достигнут пика.  1 = вкл., 0 = выкл.
# новое, начиная с версии 0.6.0, автоматическое определение низких значений (динамический диапазон)
# «Перерегулирование» позволяет столбикам выходить за пределы (в % от высоты терминала) без запуска авточувствительности.  УСТАРЕЛО с версии 0.6.0
; autosens = 1
; overshoot = 20

# Ручная чувствительность в %.  Если функция autosens включена, это будет только начальное значение.
# 200 означает двойную высоту.  Принимает только неотрицательные значения.
; sensitivity = 100

# Количество баров (0-512).  0 устанавливает автоматический режим (заполняет консоль).
# Ширина столбцов и расстояние между столбцами в количестве символов.
; bars = 0
; bar_width = 2
; bar_spacing = 1
# bar_height is only used for output in "noritake" format
; bar_height = 32

# Для ширины SDL и расстояния между полосами в пикселях значения по умолчанию:
; bar_width = 20
; bar_spacing = 5

# sdl_glsl имеет эти значения по умолчанию, они используются только для расчета максимального количества баров.rs.
; bar_width = 1
; bar_spacing = 0

# Более низкие и высокие частоты среза для самых низких и самых высоких тактов
# пропускная способность визуализатора.
# Примечание: минимальная общая полоса пропускания составляет 43 МГц x количество полосок.
# Cava автоматически увеличит верхнюю границу среза, если указана слишком низкая полоса.
; lower_cutoff_freq = 50
; higher_cutoff_freq = 10000

# Секунды без ввода данных до того, как Cava перейдет в спящий режим.  Cava не будет выполнять БПФ или рисовать и
# проверяйте ввод только один раз в секунду.  Кава проснется, как только будет обнаружен ввод.  0 = отключить.
; sleep_timer = 0

[input]

# Метод захвата звука.  Возможные методы: «пульс», «алса», «фифо», «сндио» или «шмем».
# По умолчанию «pulse», «pipewire», «alsa» или «fifo» в указанном порядке, в зависимости от того, с какой поддержкой была создана Cava.
# На Mac по умолчанию используется значение «portaudio» или «fifo».
# В Windows это происходит автоматически и никаких настроек ввода не требуется.

# Все методы ввода используют одну и ту же переменную конфигурации «source».
# чтобы определить, откуда он должен получить звук.

# Для PulseAudio и Pipewire источником будет источник.  По умолчанию: «авто», который использует источник монитора приемника по умолчанию.
# (все приемники (выходы) импульсного аудио имеют связанные с ними «мониторные» источники (входы).

# Для piepwire «источником» будет имя объекта или object.serial устройства, с которого осуществляется захват.
# Поддерживаются как устройства ввода, так и вывода.

# Для alsa «источником» будет устройство захвата.
# Для fifo 'source' будет путь к fifo-файлу.
# Для shmem «источником» будет /squeezelite-AA:BB:CC:DD:EE:FF, где «AA:BB:CC:DD:EE:FF» будет MAC-адрес Squeezlite
method = pulse
source = auto

; method = pipewire
; source = auto

; method = alsa
; source = hw:Loopback,1

; method = fifo
; source = /tmp/mpd.fifo
; sample_rate = 44100
; sample_bits = 16

; method = shmem
; source = /squeezelite-AA:BB:CC:DD:EE:FF

; method = portaudio
; source = auto

[output]

# Метод вывода.  Могут быть «ncurses», «noncurses», «raw», «noritake», «sdl».
# или 'sdl_glsl'.
# 'noncurses' использует специальную технику фреймбуфера и печатает только изменения
# от кадра к кадру в терминале.  «ncurses» используется по умолчанию, если поддерживается.
#
# «raw» — это 8- или 16-битные данные (настраиваются с помощью опции «bit_format»).
# поток высот полос, который можно использовать для отправки в другие приложения.
# «raw» по умолчанию составляет 200 баров, которые можно настроить с помощью опции «бары» выше.
#
# 'noritake' выводит растровое изображение в формате, ожидаемом для дисплея Noritake VFD.
# в графическом режиме.  На данный момент он поддерживает только графические VFD серии 3000.
#
# 'sdl' использует простой слой DirectMedia для рендеринга в графическом контексте.
# 'sdl_glsl' использует SDL для создания контекста OpenGL.  Напишите свои собственные шейдеры или
# используйте один из предопределенных.
; method = ncurses

# Ориентация визуализации.  Может быть «снизу», «сверху», «слева» или «справа».
# По умолчанию — «низ».  Другие ориентации поддерживаются только на sdl и ncruses.
# выход.  Примечание: во многих шрифтах есть странные глифы для «верхних» и «правых» символов.
# из-за чего ncurses может выглядеть неправильно.
; orientation = bottom

# Визуальные каналы.  Может быть «стерео» или «моно».
# «стерео» отражает оба канала с низкими частотами в центре.
# 'mono' выводит слева направо самые низкие и самые высокие частоты.
# 'mono_option' устанавливает моно для приема входных данных «слева», «права» или «среднего».
# установите для параметра «reverse» значение 1, чтобы отображать частоты наоборот.
; channels = stereo
; mono_option = average
; reverse = 0

# Необработанная цель вывода.  Fifo будет создан, если цель не существует.
; raw_target = /dev/stdout

# Формат необработанных данных.  Может быть «двоичным» или «ascii».
; data_format = binary

# Двоичный битовый формат, может быть «8 бит» (0–255) или «16 бит» (0–65530).
; bit_format = 16bit

# Максимальное значение Ascii.  В режиме «ascii» диапазон будет варьироваться от 0 до значения, указанного здесь.
; ascii_max_range = 1000

# Разделители Ascii.  В формате ascii каждая полоса и кадр разделены разделителями.
# Используйте десятичное значение в таблице ascii (т.е. 59 = ';' и 10 = '\n' (перевод строки)).
; bar_delimiter = 59
; frame_delimiter = 10

# размер и положение окна sdl.  -1,-1 центрировано.
; sdl_width = 1000
; sdl_height = 500
; sdl_x = -1
; sdl_y= -1

# установить метку на стержнях по оси X.  Может быть «частота» или «нет».  По умолчанию: «нет»
# «частота» отображает нижнюю частоту среза полосы выше.
# Поддерживается только для вывода ncurses и noncurses.
; xaxis = none

# включить синхронизированные обновления alacritty.  1 = вкл., 0 = выкл.
# убирает мерцание в эмуляторе терминала alacritty.
# по умолчанию отключено, поскольку поведение в других эмуляторах терминала неизвестно
; alacritty_sync = 0

# Шейдеры для sdl_glsl, находятся в $HOME/.config/cava/shaders.
; vertex_shader = pass_through.vert
; fragment_shader = bar_spectrum.frag

; for glsl output mode, keep rendering even if no audio
; continuous_rendering = 0

[color]

# Цвета могут быть одним из семи предустановленных: черный, синий, голубой, зеленый, пурпурный, красный, белый, желтый.
# Или определяется шестнадцатеричным кодом «#xxxxxx» (шестнадцатеричный код должен находиться в пределах «»).  Пользовательские цвета требуют
# терминал, который может изменять определения цвета, например Gnome-terminal или rxvt.
# по умолчанию сохраняется текущий цвет терминала
; background = default
; foreground = default

# SDL и sdl_glsl поддерживают только цвета шестнадцатеричного кода, это значения по умолчанию:
; background = '#111111'
; foreground = '#33ffff'

# Режим градиента, поддерживаются только шестнадцатеричные цвета,
# Фон также должен быть определен в шестнадцатеричном формате или оставаться закомментированным.  1 = вкл., 0 = выкл.
# Вы можете определить до 8 различных цветов.  Они располагаются снизу вверх экрана.
gradient = 1
gradient_count = 4
gradient_color_1 = '#FB00FF'
gradient_color_2 = '#00DDFF'
gradient_color_3 = '#00FF99'
gradient_color_4 = '#FF0000'
; gradient_color_5 = '#FFB300'
; gradient_color_6 = '#FF7300'
; gradient_color_7 = '#FF0000'
; gradient_color_8 = '#cc3333'

[smoothing]

# Процентное значение для интегрального сглаживания.  Принимает значения от 0 до 100.
# Более высокие значения означают более плавное, но менее точное.  0, чтобы отключить.
# УСТАРЕЛО с версии 0.8.0, вместо этого используйте шумоподавление
; integral = 77

# Отключает или включает так называемое «сглаживание Monstercat» с «волнами» или без них.  Установите значение 0, чтобы отключить.
; monstercat = 0
; waves = 0

# Установите процент гравитации для «выпадания».  Более высокие значения означают, что столбцы будут падать быстрее.
# Принимает только неотрицательные значения.  50 означает половинную плотность, 200 — двойную.  Установите значение 0, чтобы отключить «выпадение».
# УСТАРЕЛО с версии 0.8.0, вместо этого используйте шумоподавление
; gravity = 100

# По высоте столбца столбцы, которые были бы ниже, не будут нарисованы.
# УСТАРЕЛО с версии 0.8.0
; ignore = 0

# Шумоподавление, целое число 0–100. по умолчанию 77
# необработанная визуализация очень шумная, этот фактор регулирует интегральный и гравитационный фильтры, чтобы сигнал оставался плавным
# 100 будет очень медленным и плавным, 0 будет быстрым, но шумным.
; noise_reduction = 77

[eq]

# Это сложно.  Вы можете иметь столько ключей, сколько захотите.
# Не забудьте раскомментировать более одного ключа!  Больше клавиш = больше точности.
# Дополнительные пояснения и примеры см. в readme.md на github.
; 1 = 1 # bass
; 2 = 1
; 3 = 1 # midtone
; 4 = 1
; 5 = 1 # treble
